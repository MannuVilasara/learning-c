#include <stdio.h>
#include <math.h>

int main()
{
    // NOTE: Type delcaration Inscructions => declare variable before using it

    // * Types
    // int a = 2;
    // int b = a;
    // int c = b + 2;
    // int d = 1, e;

    // int x, y, z;
    // x = y = z = 2;

    // NOTE: Arithmetic Instructions [operand1(1,"abc",x) operator(+,-,*,/) operand2]
    //? for example 1 + 2, a - 2, a * b

    // int a = 1, b = 3;
    // int sum = a + b;
    // printf("%d \n", sum);

    // ! Wrong example (a + b = c) will give error ↑

    // int power = pow(2, 3);
    // printf("%d\n", power);

    // ! "^" is different operator ↑

    //* Modular Operator
    //! Doesn't work on floats
    // int a = 11, b = 3;
    // int remainder = a % b;
    // printf("%d\n", remainder);

    // * TYPE CONVERSION
    //? int op int => int
    //? int op float => float
    //? float op float => float

    // printf("%d\n", 2 * 2);
    // printf("%f\n", 2 * 2.0);
    // printf("%f\n", 2.0 * 2.0);

    // * Priority order *,/,% > +,- > =

    // printf("%d \n", 2 + 3 * 4);

    // NOTE: Control Instructions
    //* 1. Sequence Control
    //      that we learnt till now

    //* 2. Decision Control
    //      conditional statements (if,else)

    //* 3. Loop Control
    //      Loops (while,for,do-while)

    //* 4. Case Control
    //      Switch Case Statements

    return 0;
}
