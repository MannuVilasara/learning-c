#include <stdio.h>

// NOTE: Operators

int main()
{
    //* Arithmetic Operators [+,-,*,/,%]
    // int a = 10, b = 5;
    // printf("a + b = %d\n", a + b);
    // printf("a - b = %d\n", a - b);
    // printf("a * b = %d\n", a * b);
    // printf("a / b = %d\n", a / b);
    // printf("a % b = %d\n", a % b);

    //* Relational Operators [>,<,==,!=,>=,<=]
    // int a = 10, b = 5;
    // printf("a > b = %d\n", a > b);
    // printf("a < b = %d\n", a < b);
    // printf("a == b = %d\n", a == b);
    // printf("a != b = %d\n", a != b);
    // printf("a >= b = %d\n", a >= b);
    // printf("a <= b = %d\n", a <= b);

    //* Logical Operators [&& => (AND), || => (OR), ! => (NOT)]
    // int a = 10, b = 5;
    // printf("a > b && a < 15 = %d\n", a > b && a < 15);
    // printf("a < b || a < 15 = %d\n", a < b || a < 15);
    // printf("!a > b = %d\n", !(a > b));

    // NOTE: Operator Precendence
    // 1. !
    // 2. *, /, %
    // 3. +, -
    // 4. >, <, >=, <=
    // 5. ==, !=
    // 6. &&
    // 7. ||
    // 8. =

    //* Assignment Operators
    // int a = 14;
    // printf("a = %d\n", a);
    // a += 5;
    // printf("a += 5 = %d\n", a);
    // a -= 5;
    // printf("a -= 5 = %d\n", a);
    // a *= 5;
    // printf("a *= 5 = %d\n", a);
    // a /= 5;
    // printf("a /= 5 = %d\n", a);
    // a %= 5;
    // printf("a %= 5 = %d\n", a);

    //! For Future ↓
    //* Bitwise Operators [chapter: not done yet]
    //* Ternary Operators [chapter: 3]
    return 0;
}